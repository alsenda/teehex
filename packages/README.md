# packages

Contains workspace packages for the generator.

## Purpose

Hosts independently buildable packages that compose the generator system.

## Public API

No runtime API at this folder level. APIs are defined per package:

- `@teehex/cli`
- `@teehex/templates`
- `@teehex/shared`
