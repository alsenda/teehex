# @teehex/cli

CLI package for generating project scaffolds.

## Purpose

Provides the executable installer entrypoint and orchestrates template selection + file output.

## Public API

- Executable: `teehex`
- Programmatic: `run(argv)`
