# in-memory TodoRepo adapter

Baseline in-memory `TodoRepo` implementation.

## Responsibility

Stores todos in a `Map` for local development and tests.

## Public API

- `createInMemoryTodoRepo(seed?)`
