# daisyui overlay

DaisyUI-based UI overlay assets.

## Responsibility

Supplies a Vite-focused injector with Tailwind + DaisyUI config and a neobrutalist theme.

## Public API

Use `vite/overlay.json` and `vite/package.additions.json` in CLI overlay application.
