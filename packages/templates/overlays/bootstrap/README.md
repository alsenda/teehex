# bootstrap overlay

Bootstrap-based UI overlay assets.

## Responsibility

Supplies a Vite-focused injector with minimal Bootstrap integration and neobrutalist overrides.

## Public API

Use `vite/overlay.json` and `vite/package.additions.json` in CLI overlay application.
