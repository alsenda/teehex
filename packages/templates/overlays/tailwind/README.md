# tailwind overlay

Tailwind-based UI overlay assets.

## Responsibility

Supplies a Vite-focused injector with Tailwind config and neobrutalist theme utilities.

## Public API

Use `vite/overlay.json` and `vite/package.additions.json` in CLI overlay application.
