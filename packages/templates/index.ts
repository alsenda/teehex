import type { TemplateSource } from "@teehex/shared";

export const TEMPLATES: readonly TemplateSource[] = [
  {
    id: "hexagonal-ts-minimal",
    description: "Minimal TypeScript hexagonal architecture template"
  }
] as const;
