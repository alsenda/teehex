# bootstrap

Composition root for request handlers.

## Responsibility

Builds a container with adapter implementations and core use case functions.

## Adapter swapping

Update this composition root to point to different adapters without changing domain or use case modules.
