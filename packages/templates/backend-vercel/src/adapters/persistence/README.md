# persistence adapters

Todo repository implementations.

## Responsibility

Provides repository implementations and provider selection.

## Adapter swapping

Add a real DB adapter that implements `TodoRepo` and select it in `create-todo-repo.ts`.
