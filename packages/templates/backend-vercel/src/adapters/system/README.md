# system adapters

Runtime adapters for generic system ports.

## Responsibility

Implements `ClockPort`, `IdPort`, and `LoggerPort` using Node.js primitives.

## Adapter swapping

Swap implementations for deterministic testing, structured logging, or custom ID schemes.
