# src

Application runtime internals for adapter implementations and composition.

## Responsibility

Contains adapter implementations and dependency wiring consumed by transport handlers.

## Adapter swapping

Swap concrete adapters in this folder while preserving port interfaces from `src/core/ports`.
